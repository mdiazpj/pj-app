version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: Pjchile.2121
      MYSQL_DATABASE: totem-pj
      MYSQL_USER: pj-admin
      MYSQL_PASSWORD: Pjchile.2121
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
